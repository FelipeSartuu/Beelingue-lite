# Dependências principais do projeto
# Para instalar tudo, rode: pip install -r requirements.txt

# Streamlit cria a interface simples no navegador.
streamlit>=1.40.0

# python-dotenv lê variáveis do arquivo .env, como a chave da Gemini API.
python-dotenv>=1.0.1

# LangChain é usado para montar prompts e conversar com o modelo de IA.
langchain>=0.3.0
langchain-core>=0.3.0

# Integração oficial/comunitária do LangChain com os modelos Gemini do Google.
langchain-google-genai>=2.0.0

# LangGraph organiza o fluxo do agente em nós, estados e rotas.
langgraph>=0.2.0
